// Initial state
const state = {
    theme: {
        color: 'blue'
    }
}

// Getters
const getters = {}

// Actions
const actions = {}

// Mutations
const mutations = {}

export default {
    state,
    getters,
    actions,
    mutations
}